server:
  port: 8081

spring:
  application:
    name: customer-service

  datasource:
    url: jdbc:sqlserver://LAPTOP-ROIBK9BH;databaseName=Customers_Karaoke;encrypt=true;trustServerCertificate=true
    username: sa
    password: Longlatoi1409@
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.SQLServer2012Dialect

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    ip-address: 127.0.0.1

logging:
  level:
    root: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    com.example.karaoke: DEBUG
