server:
  port: 8080

spring:
  application:
    name: user-service

  datasource:
    url: jdbc:sqlserver://LAPTOP-ROIBK9BH;databaseName=User_Karaoke;encrypt=true;trustServerCertificate=true
    username: MyServer
    password: longlatoi1409
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.SQLServer2012Dialect

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    ip-address: 127.0.0.1
